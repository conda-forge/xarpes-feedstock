{% set name = "xarpes" %}
{% set version = "0.6.4" %}
{% set python_min = "3.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: f74216cc1ad0a47df440f2edacb69fd53e885a8b33780a9ee65bd5a4cc1f491d

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - flit-core >=3.4
    - pip
  run:
    - python >={{ python_min }}
    - igor2 >=0.5.8
    - jupyterlab
    - jupytext
    - matplotlib-base <3.10.0
    - numpy
    - scipy
    - lmfit
    - pyqt
    - ipympl >=0.9.3
    - ipywidgets >=8.1.5
    - ipykernel <6.32.0

test:
  imports:
    - xarpes
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://xarpes.readthedocs.io
  summary: Extraction from angle resolved photoemission spectra
  license: GPL-3.0-only
  license_file: LICENSE

extra:
  recipe-maintainers:
    - TeetotalingTom
    - janberges
    - sponce24
